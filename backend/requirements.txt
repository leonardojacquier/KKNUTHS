# Core
pydantic>=2.6
python-dotenv>=1.0

# LLM (Claude)
anthropic>=0.40

# Embeddings (escolha um provedor) + billing
voyageai>=0.2
openai>=1.30
stripe>=9.0

# API + bot
fastapi>=0.110
uvicorn[standard]>=0.29
python-telegram-bot>=21.0

# Jobs / cache
redis>=5.0
rq>=1.16

# DB
supabase>=2.4
psycopg[binary]>=3.1

# Poker math (deterministic tools + solvers)
treys>=0.1.8
numpy>=1.26

# Ingestion (optional extras, lazy-imported)
pdfplumber>=0.11
pillow>=10.0

# Dev
pytest>=8.0
